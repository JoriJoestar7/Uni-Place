{
  "name": "uniplace",
  "version": "1.0.0",
  "private": true,
  "description": "UniPlace frontend and backend deploy wrapper",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "npm --prefix server start",
    "dev": "npm --prefix server run dev",
    "postinstall": "npm --prefix server install"
  }
}
